---
phase: 07-timeout-salvage
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/agent.test.ts
  - src/bot.test.ts
autonomous: true
requirements:
  - SALVAGE-01
  - SALVAGE-02
  - SALVAGE-03
must_haves:
  truths:
    - "agent.test.ts proves runAgent captures Task tool_result blocks on user events and surfaces them in AgentResult.subagentResults when the SDK stream ends in an abort"
    - "bot.test.ts proves formatTimeoutReply produces the expected reply for each combination of (subagents present/absent) x (scratchpad present/absent) x (partial text present/absent)"
    - "bot.test.ts proves the all-empty case returns the caller-provided fallback message verbatim (SALVAGE-03 acceptance)"
    - "The captured Archie 2026-05-12 GHL scratchpad content is used as a realistic fixture so the formatter is exercised on real-world data shape, not synthetic toy strings"
  artifacts:
    - path: "src/agent.test.ts"
      provides: "New describe block for tool_result capture on abort"
      contains: "describe('runAgent tool_result capture'"
    - path: "src/bot.test.ts"
      provides: "New describe block for formatTimeoutReply"
      contains: "describe('formatTimeoutReply'"
    - path: "src/bot.test.ts"
      provides: "Embedded Archie scratchpad fixture (GHL workflows API findings, ~2.7KB)"
      contains: "ARCHIE_SCRATCHPAD_FIXTURE"
  key_links:
    - from: "src/agent.test.ts"
      to: "runAgent + vi.mocked(query)"
      via: "mock SDK stream yielding assistant tool_use then user tool_result then abort"
      pattern: "vi\\.mocked\\(query\\)"
    - from: "src/bot.test.ts"
      to: "formatTimeoutReply"
      via: "direct function call with synthesized AgentResult and a tmp scratchpad file"
      pattern: "formatTimeoutReply\\("
---

<objective>
Add automated coverage for the two new code paths shipped in Plans 07-01 and 07-02:
1. `runAgent`'s new `user`-event handler — assert that a mocked SDK stream emitting an `assistant` tool_use Task block followed by a `user` tool_result block, then aborting mid-stream, returns an `AgentResult` with `aborted: true` AND `subagentResults` populated with the expected content.
2. `formatTimeoutReply` — assert each of the 8 combinations of (subagents) × (scratchpad) × (partial text) presence/absence produces the expected output, including the all-empty fallback returning the caller's static string verbatim (SALVAGE-03 acceptance).

Purpose: Without these tests, SALVAGE-01..03 are theoretically satisfied but not enforceable in CI. The captured 2026-05-12 Archie scratchpad is reused as a realistic fixture so the formatter sees real-world content shape (markdown headings, bullet lists, URLs, ~2.7KB) rather than toy strings.

Output: New test cases in `src/agent.test.ts` and `src/bot.test.ts`. No new test files — extend the existing ones to keep conventions consistent (both files already use vitest and the `vi.mocked(query)` pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-timeout-salvage/07-CONTEXT.md
@.planning/phases/07-timeout-salvage/07-01-SUMMARY.md
@.planning/phases/07-timeout-salvage/07-02-SUMMARY.md
@.planning/debug/archie-subagent-timeout-loss.md
@src/agent.ts
@src/bot.ts
@src/scratchpad.ts
@src/agent.test.ts
@src/bot.test.ts

<interfaces>
<!-- Existing test conventions to mirror: -->
<!-- src/agent.test.ts:1 — import { describe, it, expect, vi, beforeEach } from 'vitest'; -->
<!-- src/agent.test.ts:27 — import { runAgentWithRetry } from './agent.js'; (we'll add runAgent + SubagentResult) -->
<!-- src/agent.test.ts:28 — import { query } from '@anthropic-ai/claude-agent-sdk'; — already mocked via vi.mock at top of file -->
<!-- src/agent.test.ts:138 — example of the abort-pre-aborted pattern; new test follows similar shape but with mid-stream events first -->

<!-- src/bot.test.ts:1-3 — already imports vitest + helpers exported from bot.ts -->
<!-- src/bot.test.ts:277 — describe('recoverFromCompactionIfNeeded' ...) shows how this file constructs synthetic UsageInfo + AgentResult for direct helper testing -->

<!-- AgentResult/SubagentResult shapes available from './agent.js' after Plan 07-01 -->
<!-- formatTimeoutReply(result, scratchpadPath, fallbackMessage): string exported from './bot.js' after Plan 07-02 -->
<!-- readScratchpad(path): string | null exported from './scratchpad.js' after Plan 07-02 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tool_result capture test to src/agent.test.ts</name>
  <files>src/agent.test.ts</files>
  <action>
Add a new `describe('runAgent tool_result capture on abort', ...)` block at the bottom of `src/agent.test.ts`. Mirror the existing `runAgentWithRetry` describe block (lines 55+) for the mocking conventions — the file already has `vi.mock('@anthropic-ai/claude-agent-sdk', ...)` set up at the top, so `vi.mocked(query)` works without further setup.

Add a new import for `runAgent` and `SubagentResult` from `'./agent.js'` alongside the existing `runAgentWithRetry` import (extend the import statement on src/agent.test.ts:27). Do not import `SubagentResult` as a value — use `type` import: `import { runAgent, runAgentWithRetry, type SubagentResult } from './agent.js'`.

Write 3 test cases inside the new describe:

**Test 1: "captures Task tool_result blocks emitted before abort"**

Setup: build an async generator that yields, in order:
1. `{ type: 'system', subtype: 'init', session_id: 'sess-test' }`
2. `{ type: 'assistant', message: { content: [{ type: 'tool_use', id: 'tu_1', name: 'Task', input: { description: 'Search GitHub for GHL workflow API patterns' } }], usage: {} } }`
3. `{ type: 'user', message: { content: [{ type: 'tool_result', tool_use_id: 'tu_1', is_error: false, content: 'Found 3 repos: ...payload...' }] } }`
4. Then either throw an AbortError or finish the iterator after the AbortController is signalled — use the same shape as the existing `:138` pre-aborted test: in the SDK mock, `throw Object.assign(new Error('aborted'), { name: 'AbortError' });` after yielding the user event, and have an AbortController in the test scope that gets `.abort()` called between the user-event yield and the throw.

Call: `const result = await runAgent('test prompt', undefined, () => {}, undefined, undefined, abortCtrl);`

Assertions:
- `result.aborted === true`
- `result.text === null`
- `Array.isArray(result.subagentResults)` — Plan 07-01 returns `[]` not `undefined` on abort, so this passes
- `result.subagentResults!.length === 1`
- `result.subagentResults![0].toolName === 'Task'`
- `result.subagentResults![0].toolUseId === 'tu_1'`
- `result.subagentResults![0].content === 'Found 3 repos: ...payload...'`
- `result.subagentResults![0].isError === false`

**Test 2: "handles array-shaped tool_result content"**

Same skeleton as Test 1, but the `tool_result.content` is an array: `[{ type: 'text', text: 'Part A' }, { type: 'text', text: 'Part B' }]`.

Assertions:
- `result.subagentResults![0].content === 'Part A\nPart B'` (joined by `\n` per Plan 07-01)

**Test 3: "skips tool_result for non-Task tool_use (Read, Grep, etc.)"**

Yield an assistant event with `tool_use.name === 'Read'` (not 'Task'), then a paired user tool_result, then abort.

Assertions:
- `result.aborted === true`
- `result.subagentResults!.length === 0` (gate on `name === 'Task'` per Plan 07-01)

**Test 4: "returns empty subagentResults array when no Task tool_use was registered"**

Yield only system + assistant text events (no tool_use), then abort.

Assertions:
- `result.aborted === true`
- `Array.isArray(result.subagentResults) && result.subagentResults!.length === 0`

Keep test setup terse — use a small helper like `function makeStream(events: unknown[], throwAfter?: number) { return async function*() { for (let i = 0; i < events.length; i++) { yield events[i]; if (i === throwAfter) throw new AbortError; } }() }`. Reuse `vi.mocked(query).mockReturnValue(makeStream([...]))` per the existing pattern at agent.test.ts:144-156.

Do NOT mock `readEnvFile` or scrub-env utilities — they read disk but tolerate missing files (they're called in every existing test).

Do NOT add tests for the success-path return — that's covered implicitly by the existing tests at lines 55+ which now need to assert `subagentResults` exists if they care, but no existing test relies on its absence.
  </action>
  <verify>
    <automated>npx vitest run src/agent.test.ts</automated>
  </verify>
  <done>
- 4 new test cases under `describe('runAgent tool_result capture on abort', ...)`
- All 4 pass
- Existing tests in src/agent.test.ts still pass (no regressions)
- Tests use the same `vi.mocked(query).mockReturnValue(asyncGenerator)` convention as the existing pre-aborted test at :144-156
- No `console.log` left behind; no `.only` or `.skip`
  </done>
</task>

<task type="auto">
  <name>Task 2: Add formatTimeoutReply tests + Archie scratchpad fixture to src/bot.test.ts</name>
  <files>src/bot.test.ts</files>
  <action>
Add a new `describe('formatTimeoutReply', ...)` block at the bottom of `src/bot.test.ts`. Extend the existing import at line 2 to bring in `formatTimeoutReply` from `'./bot.js'`. Extend the type import at line 3 to bring in `SubagentResult` from `'./agent.js'`.

Add an embedded fixture constant near the top of the new describe block:

```typescript
// Captured 2026-05-12 14:14 ET from ~/.claudeclaw/scratch/archie-5005645513-1778594387139.md
// 2.7KB of GHL workflows API research that sat on disk while the user saw the bare timeout message.
// Used to verify formatTimeoutReply handles realistic content shape (markdown, URLs, bullets).
const ARCHIE_SCRATCHPAD_FIXTURE = `## GHL Workflows API — Findings

**Base URLs:**
- V2 (current): https://services.leadconnectorhq.com
- V1 (EOL Dec 2025): https://rest.gohighlevel.com

**Auth:** Bearer JWT + \`Version: 2021-07-28\` header + \`locationId\` query param.

**Endpoints (confirmed):**
- GET /workflows/ — list workflows for a location
- PUT /workflows/{id}/status — change workflow status
- POST /workflows/{id}/trigger — marketplace trigger execute

**Trigger subscription payload:** \`{ eventType, eventData: {...}, locationId, contactId? }\`

**Rate limits:** 100 req / 10s per resource.
**Required scopes:** workflows.readonly

**Sources checked:**
- https://highlevel.stoplight.io/docs/integrations
- https://github.com/GoHighLevel/highlevel-api-docs

**Next: need to find** webhook signature scheme + retry semantics.
`;
```

The exact byte count does not need to match the captured 2730 bytes — what matters is that the fixture has realistic shape (markdown headings, code spans, URLs, bullet lists, ~2KB scale). Embed inline as a `const` rather than reading from disk.

Helper for tests:

```typescript
function makeUsage(overrides: Partial<UsageInfo> = {}): UsageInfo {
  return {
    inputTokens: 100,
    outputTokens: 50,
    cacheReadInputTokens: 0,
    totalCostUsd: 0.001,
    didCompact: false,
    preCompactTokens: null,
    lastCallCacheRead: 0,
    lastCallInputTokens: 100,
    subtype: 'success',
    numTurns: 12,
    ...overrides,
  };
}
```

(Adjust field names to match the actual UsageInfo interface — re-read it from `src/agent.ts` to confirm exact shape before writing.)

For the scratchpad parameter: tests pass either `null` (no scratchpad) or a real tmp file path created via `fs.mkdtempSync(os.tmpdir() + '/timeout-test-')`. Write the fixture into the tmp file with the same header format `createScratchpad` uses (first line `# Scratchpad for ${agentId} / chat ${chatId}\n\n` then body), so `readScratchpad` strips the header per its contract. Use `afterEach` to clean up tmp files.

A simpler alternative: parametrize `readScratchpad` via vitest's `vi.mock('./scratchpad.js', ...)` to return a predetermined string per test, avoiding tmp-file management. **Either approach is fine — prefer the real-file approach** since it doubles as integration coverage of `readScratchpad`'s header-stripping behavior shipped in Plan 07-02.

Test cases (each builds an `AgentResult`, calls `formatTimeoutReply(result, scratchpadPath, fallbackMessage)`, asserts on the returned string):

**Test 1: "all three salvage sources empty → returns fallback verbatim"**
- `result = { text: null, newSessionId: undefined, usage: makeUsage(), aborted: true, subagentResults: [] }`
- `scratchpadPath = null`
- `fallbackMessage = 'Timed out after 900s. Try smaller steps.'`
- Assert: `output === fallbackMessage` (byte-identical, SALVAGE-03 acceptance).

**Test 2: "header-only scratchpad with empty subagents and null text → returns fallback verbatim"**
- Same as Test 1 but scratchpadPath points to a tmp file containing ONLY the header line (`# Scratchpad for archie / chat 123\n\n`).
- Assert: returns fallback verbatim (proves `readScratchpad` returns `null` for header-only and that the fallback path then fires).

**Test 3: "subagents only"**
- One Task SubagentResult with content `'Found 3 repos: ...'`.
- Empty scratchpad path (`null`), `result.text = null`.
- Assert: output contains `'## Subagent: Task'`, contains `'Found 3 repos: ...'`, does NOT contain `'## Scratchpad'`, does NOT contain `'## Partial work'`, contains a footer line mentioning recovery count.

**Test 4: "scratchpad only — uses the Archie fixture"**
- `subagentResults = []`, `result.text = null`.
- Scratchpad tmp file contains the header + ARCHIE_SCRATCHPAD_FIXTURE.
- Assert: output contains `'## Scratchpad'`, contains the substring `'V2 (current): https://services.leadconnectorhq.com'` (from fixture), contains `'workflows.readonly'`, does NOT contain `'## Subagent'`.

**Test 5: "partial text only"**
- `subagentResults = []`, `scratchpadPath = null`, `result.text = 'Started researching GHL...'`.
- Assert: output contains `'## Partial work'`, contains `'Started researching GHL...'`.

**Test 6: "all three sources present"**
- 2 subagent results, scratchpad with fixture, `result.text = 'Conclusion in progress...'`.
- Assert: output contains all three section headings in order — find the index of each section heading and assert `subagentIdx < scratchpadIdx < partialIdx` per CONTEXT precedence locked at "Formatter precedence + empty fallback".

**Test 7: "footer includes recovered count and turn count when usage is present"**
- 2 subagent results, `usage.numTurns = 18`.
- Assert: output contains `'2'` near `'subagent'` (recovered count) AND contains `'18'` near `'turn'` (turn count).

**Test 8: "footer omits turn clause when usage.numTurns is 0 or missing"**
- 1 subagent, `usage = null`.
- Assert: output does NOT contain `'turn'` substring in the footer line — match precisely whatever Plan 07-02 chose for the "no turns" footer wording. (If Plan 07-02 always includes "turn" regardless, relax this test to check the count is present.)

**Test 9: "array-shaped subagent content rendered verbatim"**
- 1 subagent with `content = 'Part A\nPart B'` (already-joined per Plan 07-01).
- Assert: output contains both `'Part A'` and `'Part B'`.

**Test 10: "error subagent labelled"**
- 1 subagent with `isError = true`.
- Assert: output contains the `(error)` annotation in the section heading, OR whatever Plan 07-02 chose for the error label.

Wire up `beforeEach` to clean state and `afterEach` to remove tmp files. Use `fs.mkdtempSync` + `fs.rmSync(..., { recursive: true })` per the existing convention in the codebase (grep `mkdtempSync` for analogs — `src/media.test.ts` or `src/avatars.test.ts` likely show the pattern).

Do NOT test through the real bot loop, real ctx, real emitChatEvent, or real Telegram API. `formatTimeoutReply` is a pure function — call it directly. Wiring tests (the call site at :718 / :1810 calling it with the right arguments) are out of scope here because they require mocking `Bot` and `ctx.reply` — the helper-level tests are the right TDD anchor per Phase 6's precedent (`recoverFromCompactionIfNeeded` is tested directly, not through `processMessage`).
  </action>
  <verify>
    <automated>npx vitest run src/bot.test.ts</automated>
  </verify>
  <done>
- 10 new test cases under `describe('formatTimeoutReply', ...)` in src/bot.test.ts
- ARCHIE_SCRATCHPAD_FIXTURE embedded as a const with realistic content shape
- All 10 tests pass
- Existing tests in src/bot.test.ts (splitMessage, extractFileMarkers, recoverFromCompactionIfNeeded) still pass
- Tmp files created in tests are cleaned up via afterEach
- No `.only` or `.skip` left behind; no `console.log`
  </done>
</task>

</tasks>

<verification>
After this plan completes:
1. `npx vitest run` (full suite) exits 0 — no regressions anywhere.
2. `npx vitest run src/agent.test.ts src/bot.test.ts` — both files green, with the new describe blocks visible in the reporter output.
3. `grep -c 'describe(' src/agent.test.ts` shows one more describe block than before this plan.
4. `grep -c 'describe(' src/bot.test.ts` shows one more describe block than before this plan.
5. `grep -n 'ARCHIE_SCRATCHPAD_FIXTURE' src/bot.test.ts` returns ≥ 2 hits (declaration + use in tests).
6. The all-empty-fallback test asserts byte-identical fallback string (SALVAGE-03 enforceable in CI).
7. The Task-only gate is enforced (Test 3 of agent.test.ts proves Read tool results are dropped).
</verification>

<success_criteria>
SALVAGE-01..03 enforceable in CI:
- Task tool_result capture on abort proved by `agent.test.ts` — SALVAGE-01.
- Scratchpad content concatenation proved by `bot.test.ts` (Tests 4 & 6) using realistic fixture — SALVAGE-02.
- All-empty fallback proved by `bot.test.ts` (Tests 1 & 2) returning the caller's static string verbatim — SALVAGE-03.

Phase 7 acceptance: full test suite green; the captured Archie GHL scratchpad content is now embedded in CI as a regression anchor against future formatter changes that might drop salvaged content.
</success_criteria>

<output>
After completion, create `.planning/phases/07-timeout-salvage/07-03-SUMMARY.md` documenting the 14 new test cases (4 + 10), the fixture provenance (2026-05-12 14:14 ET Archie GHL workflows API research), and any test cases that were dropped or merged.

Also append a one-line entry to `.planning/STATE.md` under "Pending TODOs" or equivalent: `- Phase 7 timeout salvage shipped; SALVAGE-01..03 covered in CI via src/agent.test.ts + src/bot.test.ts`.
</output>
